import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { Shell } from "@/components/ds/Shell";
import { findComparison } from "@/lib/seo/content";
const BASE = "https://deepscreen.online";
export const Route = createFileRoute("/compare/$slug")({ staticData: { sitemap: true }, loader: ({ params }) => { const comparison = findComparison(params.slug); if (!comparison) throw notFound(); return { comparison }; }, head: ({ loaderData }) => { if (!loaderData) return {}; const c = loaderData.comparison; const url = `${BASE}/compare/${c.slug}`; return { meta: [{ title: `${c.title}: Key Differences | DeepScreen` }, { name: "description", content: c.description }, { property: "og:title", content: c.title }, { property: "og:description", content: c.description }, { property: "og:type", content: "article" }, { property: "og:url", content: url }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: url }], scripts: [{ type: "application/ld+json", children: JSON.stringify({ "@context": "https://schema.org", "@graph": [{ "@type": "Article", headline: c.title, description: c.description, mainEntityOfPage: url, author: { "@type": "Organization", name: "DeepScreen" } }, { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Home", item: `${BASE}/` }, { "@type": "ListItem", position: 2, name: c.title, item: url }] }] }) }] }; }, component: ComparisonPage });
function ComparisonPage() { const { comparison: c } = Route.useLoaderData(); return <Shell><article className="mx-auto max-w-4xl px-4 py-10 sm:px-6 lg:px-8"><nav className="text-xs text-muted-foreground"><Link to="/">Home</Link> {" / "}Compare</nav><h1 className="mt-4 text-3xl font-bold">{c.title}</h1><p className="mt-5 rounded-lg border border-border bg-panel p-5 leading-relaxed">{c.answer}</p><div className="mt-8 overflow-x-auto rounded-lg border border-border"><table className="w-full min-w-[600px] text-sm"><thead className="bg-muted"><tr><th className="p-4 text-left">Difference</th><th className="p-4 text-left">{c.left}</th><th className="p-4 text-left">{c.right}</th></tr></thead><tbody className="divide-y divide-border">{c.rows.map(row => <tr key={row[0]}><th className="p-4 text-left font-medium">{row[0]}</th><td className="p-4 text-muted-foreground">{row[1]}</td><td className="p-4 text-muted-foreground">{row[2]}</td></tr>)}</tbody></table></div><p className="mt-8 text-xs text-muted-foreground">Use both measures in context. No single ratio or analysis method is sufficient for an investment decision.</p></article></Shell>; }
